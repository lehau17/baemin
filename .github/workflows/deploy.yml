name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest # Sửa lỗi chính tả

    steps:
      # Checkout repository code
      - name: Checkout code
        uses: actions/checkout@v3

      # Run a simple script
      - name: Print Hello World
        run: echo "Hello world"
